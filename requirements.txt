# Core dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
gunicorn>=21.2.0
pydantic>=2.0.0
python-multipart>=0.0.6

# PDF processing
pypdf2>=3.0.1

# Environment and configuration
python-dotenv>=1.0.0

# NLP and text processing
numpy>=1.24.0,<2.0.0  # Keep numpy < 2.0.0 for compatibility with other packages
language-tool-python>=1.0.0
nltk>=3.8.1
psutil>=5.9.0  # For system monitoring in health checks
jellyfish>=1.0.0  # For string matching and phonetic algorithms

# Machine Learning and Embeddings
torch>=2.0.0  # PyTorch for deep learning
sentence-transformers>=2.2.2  # For generating embeddings
transformers>=4.30.0  # For NLP tasks
tokenizers>=0.13.0  # Required by transformers

# Text processing and utilities
huggingface-hub>=0.16.0  # For downloading models
requests>=2.28.0  # Required by huggingface-hub
tqdm>=4.64.0  # For progress bars

# LangChain (if needed, though we're focusing on direct PDF processing)
# langchain>=0.1.0
# langchain-community>=0.0.20

# Development and testing
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.7.0
isort>=5.12.0
flake8>=6.1.0
mypy>=1.5.1
